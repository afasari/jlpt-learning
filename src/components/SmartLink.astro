---
interface Props {
  href: string;
  [key: string]: any;
}

const { href, ...props } = Astro.props;
const basePath = import.meta.env.BASE_URL || '/jlpt-learning';
const fullHref = href.startsWith('http') ? href : `${basePath}${href}`;
---

<a href={fullHref} {...props}>
  <slot />
</a>
